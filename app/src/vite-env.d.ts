/// <reference types="vite/client" />

// Injecté par vite.config.ts (define) : identifie la version du front buildée,
// pour vérifier depuis l'UI HPC quel commit/build tourne. Voir src/lib/buildInfo.ts.
declare const __BUILD_INFO__: string;
