Place the Simulation stack background video here as `simulation-stack-4k.mp4`.

The homepage also checks `simulation-stack-4k.mov` as a fallback for exported Photos videos, but MP4 is preferred for production browser support.
